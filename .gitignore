# Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Node
node_modules/
package-lock.json

# Secrets (we ship .env.example, NEVER .env)
.env
.env.local
*.secret

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Build artifacts
build/
dist/
*.egg-info/

# Slide builds — keep .pptx out, render byproducts always out
lesson*/deck/preview/
lesson*/deck/*.pdf
~$*.pptx
.~lock.*

# ─── INSTRUCTOR-ONLY (do NOT publish) ──────────────────────────
# Anything in this section is Bayram's pre-class playbook. Students
# get the Constitution + bot source + skills + the public Block 2
# README; they DON'T get the lesson plan, the planted-bug dossiers,
# the cohort roster, or the slide-deck source.

# Council / strategy docs
council-*.md

# Per-lesson instructor materials
lesson1/lesson-1-design.md
lesson1/slide-deck-outline.md
lesson1/handouts.md
lesson1/run-sheet.md
lesson1/dossiers/
lesson1/deck/
lesson1/block2-stochasticity-demo/variance-report-*.md

# When future lessons land, mirror the same pattern
lesson*/lesson-*-design.md
lesson*/slide-deck-outline.md
lesson*/handouts.md
lesson*/run-sheet.md
lesson*/dossiers/
lesson*/deck/
lesson*/staging/
lesson*/*-variance-report-*.md
lesson*/workshop*_notes*

# Lessons 3-5 not aired yet — entire dirs private until each session opens.
# Once a session airs, narrow this to per-file patterns above (matching lesson1).
lesson3/
lesson4/
lesson5/

# Lesson 2 (workshop 2026-05-30): pre-aired ship of student-facing artifacts
# only — starter templates only. Instructor playbook (design, run-sheet,
# handouts, dossiers, deck, staging, council) and internal research stay
# private per the lesson*/<pattern> rules above. Matches lesson1's footprint.
lesson2/.DS_Store
lesson2/deck/diff-fixtures.md
lesson2/deck/REVISION-BRIEF.md
lesson2/research/

# Empty/private instructor folder
instructor/

# TARS memory seeds: DENY-BY-DEFAULT. Only the Constitution (TARS's
# runtime system prompt) is public — everything else under
# memory_seeds/ may contain student data, lesson designs, instructor-
# only content, or verbatim chat quotes. New subdirs added later
# (transcripts/, personas/, etc.) inherit this default automatically.
cohort-agent/memory_seeds/*
!cohort-agent/memory_seeds/00-constitution.md

# Local TARS data
cohort-agent/data/
/tmp/
cohort-4-design.md

# Local dev byproducts (never publish)
.playwright-mcp/
cohort-agent/preview.html
cohort-agent/tars-preview.png
workspace/

# Per-machine Claude Code local settings (may contain local tool perms)
**/.claude/settings.local.json

